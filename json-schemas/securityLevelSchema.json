{
	"$schema": "http://json-schema.org/draft-04/schema",
	"title": "Schema for a UniVote security level",
	"description": "This JSON schema describes the format of the security level in the UniVote system",
	"type": "object",
	"properties": {
		"securityLevel": {
			"enum": [1, 2, 3],
			"description": "Security level of an election"
		}
	},
	"required": ["securityLevel"]
}
